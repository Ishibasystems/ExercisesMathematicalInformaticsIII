	for (i=0;i<256;i++) {
		prob[i]=0;
		mtf[i]=i;
	}
	for (i=0;i<n;i++) {
		for (j=0;j<256;j++)
			if (data[i]==mtf[j]) {
				datam[i]=j;
				mem=mtf[j];
				for (k=j;k>0;k--)
					mtf[k]=mtf[k-1];
				mtf[0]=mem;
				break;
			}
		prob[datam[i]]++;
		printf("%5d",datam[i]);
		if (i%x==0)
			printf("\n");
	}
	signal=0;
	entropy=0.0;
	for (i=0;i<256;i++)
		if (prob[i]>0) {
			pv[signal]=(double)prob[i]/(double)(x*y);
			signal++;
		}
